scenario_id: intercept_illuminate_v1
states:
  - SEARCH
  - ALIGN
  - APPROACH
  - FOLLOW_STANDOFF
  - LOST_TARGET_HOLD
  - FAILSAFE_HOLD

policy:
  ranking: inbound_nearest_hybrid
  follow_distance_m: 3.0
  follow_distance_tolerance_m: 0.4
  target_cooldown_s: 5.0
