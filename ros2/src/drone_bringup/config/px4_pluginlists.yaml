/**:
  ros__parameters:
    plugin_denylist:
      # common
      #- actuator_control
      #- ftp
      #- hil
      # extras
      #- altitude
      #- debug_value
      #- image_pub
      #- px4flow
      #- vibration
      #- vision_speed_estimate
      - guided_target   # unused in this repo; suppresses PositionTargetGlobal "no origin" warnings indoors
      - wheel_odometry
      - odometry        # suppress map_frd TF lookup errors (no external odometry source)

    # plugin_allowlist:
    #   - 'sys_*'
